# HQ (Home) — Blueprint

**Route**: `/company`
**Component**: `components/sections/hq/HQPage.tsx` (697 lines)
**Status**: Partial — UI complete, backend 30%

## What Exists

- Greeting with time-of-day + decision count
- Editable mission statement (localStorage)
- CEO Brief card (tasks completed, pending decisions, daily cost, active agents)
- Decision feed with action buttons (approve/deny/dismiss)
- Hierarchical goal tree (mission → strategic → tactical → task)
- Team strip (horizontal agent cards with status)
- Spend bar (budget utilization visualization)

## Supporting Components

| Component | File | Status |
|-----------|------|--------|
| MorningBrief | `components/company/MorningBrief.tsx` | Working (mock data) |
| DecisionFeed | `components/company/DecisionFeed.tsx` | Working (mock data) |
| DecisionCard | `components/company/DecisionCard.tsx` | Working |
| StatusStrip | `components/company/StatusStrip.tsx` | Working |
| HealthScore | `components/company/HealthScore.tsx` | Working |
| CostIndicator | `components/company/CostIndicator.tsx` | Working |
| AgentSidebar | `components/company/AgentSidebar.tsx` | Working (mock) |
| AgentPulse | `components/company/AgentPulse.tsx` | Working |
| ActivityTimeline | `components/company/ActivityTimeline.tsx` | Working |
| QuickActions | `components/company/QuickActions.tsx` | Working |

## API Endpoints

| Endpoint | Method | Status |
|----------|--------|--------|
| `/api/orchestration/goals?format=tree` | GET | Exists, returns mock |
| `/api/orchestration/goals/{id}` | PATCH | Exists (decompose action) |
| `/api/decisions/{id}` | PATCH | Exists but may fail silently |

## What Needs Work

1. **Real CEO Brief**: Fetch from `/api/core?section=hq` instead of mock
2. **Real Decisions**: Load from `/api/decisions` with Supabase
3. **Real Goals**: Wire to actual goal tree in database
4. **Real Agent Data**: Use live agent status instead of MOCK_AGENTS
5. **Decision Persistence**: Ensure PATCH actually updates database
6. **QuickActions**: Wire app-action events to AppShell handlers
7. **Activity Timeline**: Pull from audit_logs table

## Data Flow Target

```
Server page.tsx
  → fetch user, business, agents from Supabase
  → fetch decisions from Supabase
  → fetch goals from Supabase
  → pass to HQPage as props
HQPage (client)
  → renders with real data
  → decision actions → PATCH /api/decisions/{id} → refresh
  → goal decompose → PATCH /api/orchestration/goals/{id}
```
